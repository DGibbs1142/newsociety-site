// Copy this file to config.local.js and fill in your own keys.
// config.local.js is gitignored so your keys never get committed.
const NEWS_API_KEY = "your-thenewsapi-com-key-here";
const TMDB_API_KEY = "your-themoviedb-org-key-here";

// Optional — Pop Culture pulls from these too if set, but skips them
// gracefully (falls back to TMDB + TheNewsAPI only) if left undefined.
const YOUTUBE_API_KEY = "your-google-cloud-api-key-here";
const GIPHY_API_KEY = "your-giphy-api-key-here";
